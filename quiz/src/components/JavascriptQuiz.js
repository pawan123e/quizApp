import React from 'react'
import Question from './Question'

const JavascriptQuiz = ({history}) => {

    return ( <Question type='Javascript' history={history}/> )
    
}

export default JavascriptQuiz
